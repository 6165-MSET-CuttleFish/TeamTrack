class StatConfig {
  StatConfig({
    this.showPenalties = false,
    this.allianceTotal = false,
    this.removeOutliers = false,
    this.sorted = true,
  });
  bool showPenalties;
  bool allianceTotal;
  bool removeOutliers;
  bool sorted;
}
